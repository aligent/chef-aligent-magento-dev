[Unit]
Description=Create templated configs
After=<%= node['app']['templates']['after'] %>
Before=<%= node['app']['templates']['before'] %>

[Service]
Type=oneshot
ExecStart=/usr/bin/templates.sh

[Install]
WantedBy=multi-user.target
